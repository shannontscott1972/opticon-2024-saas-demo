fragment HeaderBlockData on HeaderBlock {
  site_logo { ...ReferenceData }
  site_logo_dark { ...ReferenceData }
  site_main_navigation { ...IContentListItem }
  site_utility_navigation { ...IContentListItem }
}